<%= render @list %>

<div>
  <%= link_to "Edit", edit_list_path(@list), class: "btn btn-outline-secondary btn-sm mb-1" %> |
  <%= link_to "Back", lists_path, class: "btn btn-outline-secondary btn-sm mb-1" %>

  <%= button_to "Destroy", @list, method: :delete, class: "btn btn-outline-danger btn-sm" %>
</div>
